name: ui-test-action
run-name: Check analyze and test
on: [push]

jobs:
  setup-flutter:
    name: Setup Flutter
    runs-on: ubuntu-latest
    steps:
      - name: Setup Flutter
        uses: flutter-actions/setup-flutter@v3.6
        with:
          channel: "stable"
          version: "2.19.0"

  flutte-analyze:
    name: Run Flutter analyze
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Run Public flutter analyze
        run: flutter analyze
